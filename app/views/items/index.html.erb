<%= link_to 'マイページへ行く', user_path(current_user) %>
<div class="content">
  <% @items.each do |item| %> 
    <%# eachメソッドを使うことで、全商品に対して以下の処理を実行します %>
    <p>商品名：<%= item.name %></p>
    <p>値段：<%= item.price %>円</p>
    <%= link_to '購入する'%>
  <% end %>
</div>